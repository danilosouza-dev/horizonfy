import styled from "styled-components";

export const Container = styled.main`
  max-width: 1208px;
  margin: 0 auto;
  padding: 2.5rem 0;
  position: relative;

  section {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 2.5rem;
  }
`;